import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cursoJavaIonic',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
